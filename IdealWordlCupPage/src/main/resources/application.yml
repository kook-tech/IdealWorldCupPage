spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/idealworldcup
    username: root
    password: 1234

#    ?characterEncoding=UTF-8&serverTimezone=UTC

  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: 'true'
    show-sql: 'true'
    hibernate:
      ddl-auto: create
server:
  port: 8080
